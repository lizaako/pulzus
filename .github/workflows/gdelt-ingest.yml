name: GDELT Ingest Cron

on:
  schedule:
    - cron: '7-59/15 * * * *'
  workflow_dispatch:

jobs:
  run-gdelt-ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger gdelt-ingest Supabase Edge Function
        run: |
          response_file="$(mktemp)"
          status_code="$(curl -sS -o "$response_file" -w "%{http_code}" -X POST "${{ secrets.SUPABASE_URL }}/functions/v1/gdelt-ingest" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json")"

          cat "$response_file"
          echo

          if [ "$status_code" -ge 400 ]; then
            echo "gdelt-ingest failed with HTTP $status_code"
            exit 1
          fi
